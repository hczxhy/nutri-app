<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="250dp"
    android:layout_height="350dp"
    android:layout_gravity="center"
    android:background="@color/nutri_white"
    android:gravity="center"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <TextView
        android:id="@+id/factoid_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:gravity="center"
        android:text="@string/factoid_dialog_title"
        android:textColor="@color/nutri_black"
        android:textSize="25sp" />

    <ProgressBar
        android:id="@+id/progress_dialog_wheel"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_below="@id/factoid_title"
        android:layout_centerInParent="true"
        android:indeterminateDrawable="@drawable/custom_progress_wheel" />

    <TextView
        android:id="@+id/factoid_text"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress_dialog_wheel"
        android:layout_centerHorizontal="true"
        android:textColor="@color/nutri_black"
       android:textSize="19sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:orientation="horizontal" >

        <ImageButton
            android:id="@+id/factoid_prev"
            android:layout_width="50dip"
            android:layout_height="50dip"
            android:layout_marginRight="50dip"
            android:background="@color/nutri_white"
            android:contentDescription="@string/prev_button_label"
            android:scaleType="fitCenter"
            android:src="@drawable/factoid_previous" />

        <ImageButton
            android:id="@+id/factoid_next"
            android:layout_width="50dip"
            android:layout_height="50dip"
            android:background="@color/nutri_white"
            android:contentDescription="@string/next_button_label"
            android:scaleType="fitCenter"
            android:src="@drawable/factoid_next" />
    </LinearLayout>

</RelativeLayout>